App\Entity\Category:
  attributes:
    normalization_context:
      groups: ['category_read']

    denormalization_context:
      groups: ['category_write']

  collectionOperations:
    get:
      method: 'GET'
      security: 'is_granted("ROLE_READ_CATEGORY") or is_granted("ROLE_GOD")'

    post:
      menthod: 'POST'
      security: 'is_granted("ROLE_CREATE_CATEGORY") or is_granted("ROLE_GOD")'

  itemOperations:
    get:
      method: 'GET'
      security: 'is_granted("CREATED", object)'

    put:
      method: 'PUT'
      security: 'is_granted("EDITED", object)'

    delete:
      method: 'DELETE'
      security: 'is_granted("DELETE", object)'